<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wjx.dao.mapper.UserLogMapper">
    <select  id="login" resultType="org.wjx.dto.resp.UserLoginRespDTO">
        select id as user_id ,username,real_name
        from t_user
        <where>
            <if test="usernameOrMailOrPhone!='' or usernameOrMailOrPhone!=null ">
                (username=#{usernameOrMailOrPhone} or mail=#{usernameOrMailOrPhone} or phone=#{usernameOrMailOrPhone})
            </if>
            and password=#{password} and del_flag=0
        </where>
    </select>
</mapper>